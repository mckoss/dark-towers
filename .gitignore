node_modules
.DS_Store
Thumbs.db

# Build output
/.svelte-kit
/build

# Secrets & local config
config.json
.env
.env.*
!.env.example

# Runtime data. The raw FlightAware cache lives on the Railway volume / your machine,
# not in git; `npm run db:seed` loads a small fixture from tests/fixtures/raw.
/data/

vite.config.js.timestamp-*
vite.config.ts.timestamp-*
test-results/
playwright-report/
